# NOTE: Upgrade pip first if you encounter installation issues:
#   pip install --upgrade pip
#
# Core Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data Validation
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]==2.10.3  # Email validation

# HTTP Client & Async Support
httpx==0.28.0
aiofiles==24.1.0  # Async file operations
python-multipart==0.0.20  # Form data & file uploads

# Database
sqlmodel==0.0.22
alembic==1.14.0
greenlet==3.2.4  # Required for async SQLAlchemy


# PostgreSQL drivers (install if using PostgreSQL)
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# MySQL drivers (install if using MySQL)
mysqlclient==2.2.6  # For Alembic migrations (provides MySQLdb)
pymysql==1.1.1
aiomysql==0.2.0
cryptography==44.0.0  # Required by pymysql

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.2.1  # Compatible with passlib 1.7.4

# Utilities
python-dateutil==2.9.0  # Date/time utilities
pytz==2024.2  # Timezone support
python-slugify==8.0.4  # URL-friendly slugs

# Templates (for emails, error pages, etc)
jinja2==3.1.5

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
faker==33.1.0  # Generate fake data for tests
httpx==0.28.0  # Already included above for testing
factory-boy==3.3.1  # Test fixtures
aiosqlite==0.20.0  # SQLite async driver for tests

# Code Quality
black==24.10.0
ruff==0.8.2
mypy==1.13.0
pre-commit==4.0.1  # Git hooks for code quality

# Environment Variables
python-dotenv==1.0.1

# CLI Tool
typer==0.15.1
rich==13.9.4

# ============================================
# OPTIONAL PACKAGES (Install as needed)
# ============================================

# Email Sending (uncomment if needed)
# emails==0.6

# Caching with Redis (uncomment if needed)
# redis==5.2.1

# Background Tasks (uncomment if needed)
# celery==5.4.0
# flower==2.0.1  # Celery monitoring

# Image Processing (uncomment if needed)
# pillow==11.0.0

# Error Tracking (uncomment if needed)
# sentry-sdk[fastapi]==2.19.2

# Rate Limiting (uncomment if needed)
# slowapi==0.1.9

# API Documentation Enhancements (uncomment if needed)
# markdown==3.7  # Markdown in API docs

# Documentation Site (uncomment if needed)
# mkdocs==1.6.1
# mkdocs-material==9.5.44
# mkdocs-minify-plugin==0.8.0
# pymdown-extensions==10.12
